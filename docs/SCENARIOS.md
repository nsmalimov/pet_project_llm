# Scenario matrix (generated by `PROOFLINE_WRITE_MATRIX=1 go test ./internal/engine -run TestScenarioMatrix`)

Every row is a real engine run: git worktree, real `go test -v`, real commits, scripted developer/reviewer. Verdicts are derived, never hardcoded.

| scenario | expected | actual | claims | gaps | contradictions |
|---|---|---|---|---|---|
| A — valid bugfix: baseline fails → fix → regression passes → challenger finds nothing | **SUPPORTED** | **SUPPORTED** | problem_reproduced=supported<br>root_cause_supported=supported<br>change_verified=supported<br>independent_challenge=supported<br>integration_checked=insufficient<br>cross_service_impact=insufficient | 4 |  |
| B1 — fake fix: developer inverts the failing assertion instead of fixing the code; suite passes | **BLOCKED** | **BLOCKED** | problem_reproduced=supported<br>root_cause_supported=insufficient<br>change_verified=contradicted<br>independent_challenge=supported<br>integration_checked=insufficient<br>cross_service_impact=insufficient | 5 | Change verified: The ORIGINAL tests fail against the changed code (`go test -run TestReserveRejectsSameUTCDayAcrossTimezones ./...`, exit 1); only the author's rewritten tests pass. |
| B2 — fake fix: developer skips the failing test; exit 0, nothing proven | **BLOCKED** | **BLOCKED** | problem_reproduced=supported<br>root_cause_supported=insufficient<br>change_verified=contradicted<br>independent_challenge=supported<br>integration_checked=insufficient<br>cross_service_impact=insufficient | 5 | Change verified: The ORIGINAL tests fail against the changed code (`go test -run TestReserveRejectsSameUTCDayAcrossTimezones ./...`, exit 1); only the author's rewritten tests pass. |
| C — regression: the bug is fixed but another behaviour breaks (all days collapse into one slot) | **BLOCKED** | **BLOCKED** | problem_reproduced=supported<br>root_cause_supported=supported<br>change_verified=contradicted<br>independent_challenge=insufficient<br>integration_checked=insufficient<br>cross_service_impact=insufficient | 3 | Change verified: `go test ./...` FAILS after the change (exit 1). |
| D — insufficient verification: no tests exist, nothing can be reproduced or verified; review approves | **INSUFFICIENT** | **INSUFFICIENT** | problem_reproduced=insufficient<br>root_cause_supported=insufficient<br>change_verified=insufficient<br>independent_challenge=supported<br>integration_checked=insufficient<br>cross_service_impact=insufficient | 7 |  |
| E — reviewer counterexample: fix + tests look right, independent challenger finds a concrete breaking scenario | **BLOCKED** | **BLOCKED** | problem_reproduced=supported<br>root_cause_supported=supported<br>change_verified=supported<br>independent_challenge=contradicted<br>integration_checked=insufficient<br>cross_service_impact=insufficient | 3 | Independent challenge: Reviewer requested changes with 1 finding(s). Counterexample: Reserve("101", 2026-03-14T09:00-04:00) then Reserve("101", 2026-03-14T23:30-04:00) both succeed |
| F — stale evidence: verified change, then the code changes again after verification | **STALE** | **STALE** | problem_reproduced=supported<br>root_cause_supported=insufficient<br>change_verified=stale<br>independent_challenge=stale<br>integration_checked=insufficient<br>cross_service_impact=insufficient | 5 |  |
